// 命名导出
export const sayHello = (msg) => console.log(msg);
export const formatDate = (date) => { /* ... */ };
export const API_URL = 'https://api.example.com';

// 导入方式
import { sayHello, formatDate, API_URL } from './utils'; 